/**
 * Structure to hold the current series and its details to be displayed in the
 * view.
 */
export interface Display {
  /**
   * The current series being displayed in the view.
   */
  series: string;
  /**
   * The details of the current series being displayed in the view.
   */
  details: string;
}
